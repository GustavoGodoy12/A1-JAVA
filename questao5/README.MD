Singleton thr-safe.

simulei usando o system.out

usei o padrao com o idiom holder pra que uma unica instancia de applogger segura

e o getinstance() sempre vai retornar a mesma instancia que é pega e usada por todas as threds